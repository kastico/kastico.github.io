
---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog');
const latestPosts = allPosts
  .sort((a, b) => b.data.pubDate - a.data.pubDate)
  .slice(0, 4); // mostra os 4 mais recentes

// Extrai todas as tags únicas
const allTags = [...new Set(allPosts.flatMap(post => post.data.tags || []))];
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body class="bg-gray-50 text-gray-900 dark:bg-gray-900 dark:text-gray-100">
    <Header />

    <main class="max-w-3xl mx-auto p-6 space-y-16">
      <!-- INTRO -->
      <section class="space-y-4 text-center">
        <h1 class="text-4xl font-bold">🪖 The Bunker</h1>
        <p class="italic text-gray-500">Why this space exists</p>

        <p>
          <em>The Bunker</em> — is my digital shelter.<br />
          A place to store knowledge, projects, thoughts, and experiments.<br />
          A collection of <strong>vaults</strong> — each one focused on a part of what I love:
          design, development, music, and more.
        </p>

        <p>
          This is where I’ll write, build, and refine my craft.<br />
          Sometimes code. Sometimes feelings.<br />
          But always with intent.
        </p>
      </section>

      <!-- DIFERENÇA ENTRE VAULTS E BLOG -->
      <section class="bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-sm">
        <h2 class="text-2xl font-semibold mb-3">Vaults vs Blog</h2>
        <p>
          The <strong>Vaults</strong> are where I keep complete and finished projects — things that have reached
          a final form.
        </p>
        <p class="mt-2">
          The <strong>Blog</strong> is more like an open notebook: thoughts, small experiments, and reflections.
          Some of these may eventually evolve into final projects stored in the Vaults.
        </p>
      </section>

      <!-- PESQUISA POR TAGS -->
      <section>
        <h2 class="text-2xl font-semibold mb-4">Search by Tag</h2>
        <div class="flex flex-wrap gap-2">
          {allTags.map(tag => (
            <a
              href={`/tags/${tag.toLowerCase()}/`}
              class="px-3 py-1 bg-gray-200 dark:bg-gray-700 rounded-full text-sm hover:bg-gray-300 dark:hover:bg-gray-600 transition"
            >
              #{tag}
            </a>
          ))}
        </div>
      </section>

      <!-- ÚLTIMOS POSTS -->
      <section>
        <h2 class="text-2xl font-semibold mb-4">Latest Posts</h2>
        <ul class="space-y-6">
          {latestPosts.map(post => (
            <li>
              <a href={`/blog/${post.slug}/`} class="block group">
                <h3 class="text-xl font-medium group-hover:text-blue-500 transition">
                  {post.data.title}
                </h3>
                <p class="text-gray-500 text-sm">
                  {post.data.pubDate.toLocaleDateString()}
                </p>
                <p class="mt-1">{post.data.description}</p>
                {post.data.tags && (
                  <div class="flex flex-wrap gap-2 mt-2">
                    {post.data.tags.map(tag => (
                      <span class="text-xs bg-gray-200 dark:bg-gray-700 rounded-full px-2 py-0.5">
                        #{tag}
                      </span>
                    ))}
                  </div>
                )}
              </a>
            </li>
          ))}
        </ul>
      </section>
    </main>

    <Footer />
  </body>
</html>

